# 小圆 PTE 突击宝藏资料自动交付系统 SPEC

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 1.0 |
| 文档状态 | 需求已确认，可进入技术设计与开发 |
| 更新日期 | 2026-07-30 |
| 目标域名 | `bzzl.ysspark.cn` |
| 业务时区 | `Asia/Shanghai`（UTC+8） |

本文档是小圆 PTE 突击宝藏资料自动交付系统的产品与工程需求基线。文档记录当前已确认的业务规则、页面流程、数据约束、文件生命周期、安全边界、后台范围和验收标准。

### 1.1 命令与路径文档规范

- 所有本地开发、测试和运维文档中的平台相关命令，都必须同时提供 Windows PowerShell 与 Linux/macOS shell 版本。
- 在所有平台上语法完全相同的 npm、Node.js 等命令可以只展示一次，但必须明确标注为跨平台命令。
- 平台相关的本机路径必须同时给出 Windows 的 `\` 分隔形式与 Linux/macOS 的 `/` 分隔形式，或使用经验证可在三类平台中工作的仓库相对路径。
- `/etc`、`/var`、`/opt` 等生产绝对路径和 systemd 相关命令属于远端 Debian 部署目标；文档必须明确执行环境，无需转换为本机 Windows 路径。

## 2. 项目目标

建设一套面向小红书资料买家的自动交付系统。管理员上传无水印的 PTE 复习资料并发布后，系统为每个商品入口生成独立链接；买家通过中国大陆手机号、短信验证码和小红书订单号完成验证，系统为其生成逐页带手机号水印的专属 PDF 或合集 ZIP，并在有效期内支持重复下载同一份文件。

核心目标：

1. 将上传、发布、验证、水印生成和下载串联成完整自动化流程。
2. 每次发布产生全新的资料版本和链接，历史链接无法获取后续版本资料。
3. 同一买家成功生成后复用原文件，避免重复生成。
4. 通过明确的 10 天生成窗口和 30 天链接有效期控制资料获取范围。
5. 保留必要的绑定、生成和文件记录，方便处理售后争议。

## 3. 范围

### 3.1 本期范围

- 单管理员后台登录。
- 新建资料版本。
- 上传一套完整的 5 个原始 PDF。
- 检查上传完整性并手动发布。
- 每次发布生成 6 个全新的商品入口链接。
- 展示并复制链接。
- 中国大陆手机号短信验证码。
- 小红书订单号格式检查。
- 手机号与订单号一对一绑定。
- 单项 PDF 水印生成。
- 五项 PDF 分别加水印后打包为 ZIP。
- 同浏览器自动登录。
- 换设备或登录态丢失后的重新验证。
- 生成开放、仅限下载和链接失效三个阶段。
- 生成文件保存、下载和定时归档。
- 原始文件的非公开历史保存。
- 基本版本记录和生成记录查询。

### 3.2 本期不包含

- 小红书订单真实性校验。
- 小红书订单数据导入或 API 对接。
- 小红书商品自动发货链接自动更新。
- 国际手机号和国际短信。
- 多管理员、管理员注册、找回密码和权限系统。
- 买家自行修改订单号或手机号绑定。
- 后台解除绑定、修改订单号或重新开放生成。
- 数据库历史记录自动清理。
- `old-sold` 的自动永久删除。
- 已发布错误资料的撤回、更正或替换工作流。
- 合集入口自动授予五个单项入口权限。
- 邮件发货。

上述范围外问题统一由买家联系小圆人工处理。页面只展示“请联系小圆”类文字，不增加微信号、二维码或其他联系方式。

## 4. 名词定义

| 名词 | 定义 |
| --- | --- |
| 资料版本 | 一次完整上传并发布的 5 个原始 PDF 及其元数据 |
| 商品入口 | WFD、DI、SST、RS、WE 或五项合集中的一个买家入口 |
| 入口链接 | 某个资料版本下某个商品入口对应的唯一随机 URL |
| 生成窗口 | 从发布时间起连续 240 小时，可创建新专属文件的时间段 |
| 下载窗口 | 从发布时间起连续 720 小时，已生成用户可下载文件的时间段 |
| 绑定 | 在指定资料版本和商品入口下建立手机号与订单号的一对一关系 |
| 专属文件 | 已逐页添加完整手机号水印的单项 PDF 或合集 ZIP |
| 自动登录 | 同一浏览器持有有效安全 Cookie 时，无需再次输入验证码和订单号即可访问已生成文件 |
| `old-sold` | 保存超过生成时间 30 天的历史专属文件的非公开归档目录 |

## 5. 资料和商品入口

每个资料版本必须包含以下 5 个原始 PDF，且一种题型只有一个 PDF：

| 资料类型 | 文件要求 | 商品入口主题色 |
| --- | --- | --- |
| WFD | 1 个 PDF | `#0066FF` |
| DI | 1 个 PDF | `#FF6D01` |
| SST | 1 个 PDF | `#FFA500` |
| RS | 1 个 PDF | `#FF87B8` |
| WE | 1 个 PDF | `#8A3FFC` |
| 五项合集 | 由上述 5 个 PDF 动态生成 ZIP | `#0E7C82` |

品牌主色为 `#EF4F5C`。

五项合集的固定内容为：WFD、DI、SST、RS、WE。

每次发布生成以下 6 个入口：

1. WFD。
2. DI。
3. SST。
4. RS。
5. WE。
6. 五项合集。

## 6. 角色与权限

### 6.1 管理员

管理员可以：

- 登录简单后台。
- 新建资料版本草稿。
- 上传和替换草稿中的 5 个原始 PDF。
- 查看文件检查结果。
- 发布文件完整的资料版本。
- 查看每次发布生成的 6 个链接。
- 一键复制链接。
- 查看历史版本、发布时间、生成截止时间、失效时间和状态。
- 查看基本的绑定与生成记录。

管理员不能在本期后台中：

- 修改买家订单号。
- 解除手机号和订单号绑定。
- 允许买家重新生成。
- 更改已发布版本的原始资料。
- 清理数据库记录。

### 6.2 买家

买家可以在规则允许时：

- 打开商品入口链接。
- 使用中国大陆手机号获取验证码。
- 输入短信验证码和小红书订单号。
- 首次生成专属文件。
- 在有效期内重复下载已经生成的原文件。
- 在同一浏览器中使用自动登录。

## 7. 资料版本发布流程

### 7.1 创建与上传

1. 管理员登录后台。
2. 点击“新建资料版本”。
3. 系统创建状态为 `DRAFT` 的版本。
4. 后台展示 WFD、DI、SST、RS、WE 五个明确标注的上传位置。
5. 管理员上传 5 个无水印原始 PDF。
6. 系统逐项检查文件。
7. 全部检查通过后启用“发布”按钮。

上传规则：

- 每个题型只能存在一个当前草稿文件。
- 草稿文件允许覆盖。
- 只接受 PDF。
- 加密或需要密码的 PDF 必须拒绝。
- 文件必须可正常打开并至少包含一页。
- 无水印原始文件不得通过公网静态 URL 暴露。

### 7.2 发布

管理员点击“发布”后，系统必须在一个完整事务或等效的原子流程中：

1. 再次确认 5 个 PDF 均存在且检查通过。
2. 记录准确的发布时间 `published_at`。
3. 计算 `generation_deadline = published_at + 240 小时`。
4. 计算 `expires_at = published_at + 720 小时`。
5. 为 6 个商品入口分别生成全新的、不可预测的随机令牌。
6. 生成 6 个入口链接。
7. 将版本状态更新为已发布。
8. 在后台展示 6 个可复制链接和三个关键时间。

上传完成不会自动对外生效。只有管理员点击“发布”才开始计时并开放链接。

### 7.3 发布频率

- 业务通常每周发布一批资料。
- 系统不限制为每周一次。
- 同一周允许发布多个完整版本。
- 每次发布都产生新的版本、6 个新链接和独立的 10/30 天时间窗口。
- 多个版本可以同时有效，互不覆盖、互不失效、互不获取对方资料。
- 新版本发布不会改变任何历史入口链接的内容和有效期。

### 7.4 已发布资料

本期不开发已发布错误资料的替换、撤回或更正流程。已发布资料在系统中视为不可修改。需要处理错误资料时，由运营侧另行处理。

## 8. 链接和版本状态

业务时间统一按 `Asia/Shanghai` 解释，数据库推荐以 UTC 时间保存并在业务层转换。

版本状态按当前时间动态计算：

```text
DRAFT
  尚未发布，不可通过买家入口访问

GENERATION_OPEN
  published_at <= now < generation_deadline

DOWNLOAD_ONLY
  generation_deadline <= now < expires_at

EXPIRED
  now >= expires_at
```

边界规则：

- 在 `generation_deadline` 到达的瞬间停止新增生成。
- 在 `expires_at` 到达的瞬间停止所有公开访问和下载。
- “10 天”严格表示连续 240 小时。
- “30 天”严格表示连续 720 小时。

入口链接必须包含高熵、不可枚举的随机令牌。不得仅使用可猜测的周次、题型或自增 ID 作为授权凭据。

## 9. 买家完整流程

### 9.1 首次打开

系统根据入口令牌读取：

- 资料版本。
- 商品入口类型。
- 发布时间。
- 生成截止时间。
- 链接失效时间。
- 当前状态。
- 当前浏览器是否持有该版本和入口的有效登录会话。

处理规则：

1. `EXPIRED`：直接显示失效页，不允许获取验证码、登录、生成或下载。
2. 有有效会话且已成功生成：直接展示下载页。
3. `GENERATION_OPEN` 且没有有效会话：展示手机号、验证码和订单号表单。
4. `DOWNLOAD_ONLY` 且没有有效会话：仅允许此前已成功生成的手机号继续验证登录。

### 9.2 中国大陆手机号

系统只支持中国大陆 `+86` 的 11 位手机号。

输入方式：

- 页面固定展示 `+86`。
- 用户输入 11 位手机号。
- 校验规则：`^1[3-9]\d{9}$`。

页面必须明确展示：

> 目前仅支持中国大陆手机号。没有中国大陆手机号，请联系小圆。

### 9.3 发送短信验证码

验证码使用阿里云短信服务，参考现有 DI 项目的已验证实现。

在 `GENERATION_OPEN` 阶段：

- 格式正确的中国大陆手机号可以请求验证码。

在 `DOWNLOAD_ONLY` 阶段：

- 系统必须先检查该“资料版本 + 商品入口 + 手机号”是否存在成功生成记录。
- 存在成功生成记录才允许发送验证码。
- 从未成功生成的手机号不得获取验证码。

在 `EXPIRED` 阶段：

- 所有手机号均不得获取验证码。

默认验证码规则：

- 验证码有效期为 5 分钟。
- 同一手机号 60 秒内只能发送一次。
- 同一手机号每小时最多发送 5 次。
- 同一手机号每天最多发送 10 次。
- 同一验证码连续验证失败 5 次后立即失效。
- 验证码只能使用一次。
- 验证码不得明文存入数据库或日志。
- 需要同时设置合理的 IP 维度限流，避免短信接口被批量滥用。

在 `DOWNLOAD_ONLY` 阶段，对不满足条件的手机号使用统一文案，避免通过接口响应暴露详细生成记录：

> 本期资料的专属文件生成时间已经结束，仅限此前成功生成过文件的用户登录下载。

### 9.4 小红书订单号

已确认的小红书订单号格式为：

```regex
^P\d{18}$
```

即：

- 总长度 19 位。
- 第 1 位是大写字母 `P`。
- 后续是 18 位数字。

输入归一化规则：

- 自动移除首尾空格。
- 自动将开头的小写 `p` 转为大写 `P`。
- 不接受中间空格或其他字符。

本期只检查格式，不验证订单号是否真实存在、是否已付款或是否包含当前商品。

### 9.5 登录与绑定

首次生成前，用户必须同时满足：

1. 入口处于 `GENERATION_OPEN`。
2. 手机号格式有效。
3. 短信验证码验证成功。
4. 订单号格式有效。
5. 手机号和订单号符合一对一绑定约束。

绑定作用域为：

```text
资料版本 + 商品入口
```

在同一作用域内必须同时满足：

```text
一个手机号只能绑定一个订单号
一个订单号只能绑定一个手机号
```

数据库必须建立以下两个唯一约束：

```text
UNIQUE(material_version_id, product_entry, phone)
UNIQUE(material_version_id, product_entry, order_number)
```

允许的情况：

- 同一手机号和订单号在不同商品入口分别绑定。
- 同一手机号和订单号在不同资料版本分别绑定。
- 同一订单号进入其已绑定的原商品入口并重复下载。

不允许的情况：

- 同一作用域内，一个手机号更换另一个订单号。
- 同一作用域内，一个订单号更换另一个手机号。

用户输错并完成绑定后，本期不提供自助修改或后台修正功能，页面提示联系小圆。

### 9.6 自动登录

成功验证或生成后，系统向浏览器写入安全登录 Cookie。

自动登录的作用域是：

```text
资料版本 + 商品入口 + 已绑定用户
```

规则：

- 同一浏览器再次打开同一入口时，若会话有效且已有成功文件，直接显示下载按钮。
- 打开另一个商品入口时需要单独验证和绑定。
- 打开另一个资料版本时需要单独验证和绑定。
- 换设备、换浏览器、使用无痕模式或清除 Cookie 后，需要重新输入手机号、短信验证码和原订单号。
- 系统不使用侵入式设备指纹，因此“同一设备”在技术上以同一浏览器的有效 Cookie 为准。
- Cookie 只保存高熵随机会话令牌，不保存完整手机号和订单号。
- 服务端只保存会话令牌的安全摘要。
- Cookie 必须设置 `HttpOnly`、`Secure` 和适当的 `SameSite` 属性。
- 会话过期时间不得晚于入口链接的 `expires_at`。

换设备重新登录时必须验证：

```text
手机号 + 短信验证码 + 原订单号
```

三项全部匹配已有绑定后才允许下载。

## 10. 文件生成

### 10.1 生成触发

买家点击“生成我的专属文件”后：

1. 后端重新检查入口状态，前端时间不能作为授权依据。
2. 检查手机号验证码验证结果。
3. 归一化并检查订单号格式。
4. 在数据库事务中创建或确认手机号与订单号绑定。
5. 获取对应原始 PDF。
6. 创建唯一生成任务。
7. 对 PDF 的每一页添加水印。
8. 保存生成文件。
9. 将生成状态更新为成功。
10. 创建登录会话。
11. 返回下载页面。

### 10.2 一次生成与幂等性

同一“资料版本 + 商品入口 + 手机号 + 订单号”只能存在一份成功生成的专属文件。

- 已成功生成：直接复用原文件，不再运行水印脚本。
- 正在生成：返回同一个任务状态，不创建第二个任务。
- 生成失败：允许重试。
- 只有生成成功才进入永久的“不可重复生成”状态。
- 连续点击、页面刷新和并发请求不得生成多份文件。
- 文件名可以相同，但内部存储键必须唯一，避免不同买家文件相互覆盖。

如果已经成功生成的文件丢失或损坏，页面提示联系小圆，本期不自动重新生成。

### 10.3 前端进度

生成过程使用阶段式进度展示：

```text
正在验证信息
→ 正在添加专属水印
→ 正在整理文件
→ 生成完成
```

页面可以展示预计等待时间或进度动画，但不得在后端尚未完成时提前显示下载成功。

## 11. 水印规则

水印功能参考以下相邻项目文件（从本仓库根目录定位）：

| 平台 | 路径 |
| --- | --- |
| Windows PowerShell | `..\AIPTE\knowledge\auto\add_waterprint.py` |
| Linux/macOS | `../AIPTE/knowledge/auto/add_waterprint.py` |

每一页必须包含以下两段水印：

```text
祝考试好运 UPUP
{完整手机号}
```

强制规则：

- 订单号不写入水印。
- 手机号显示完整 11 位号码，不脱敏。
- 每一页都添加水印。
- 必须支持中文。
- 水印处理不得改变页面顺序。
- 生成结果必须保持所有页面可正常查看。

参考脚本的视觉基线：

- 大号浅色水印。
- 约 45° 倾斜。
- 订单祝福语与手机号位于两个位置。
- 黑色低透明度。
- 现有脚本约使用 72 号字体和 2% 透明度；实际 PDF 样张验收确认 2% 文字过淡，因此文字采用 6% 低透明度，中央 Logo 保持约 2%。
- 每页右下角添加小号不透明 Logo。
- 每页中央添加大号约 2% 透明度 Logo。

服务端实现要求：

- 抽取现有脚本中的 PDF 水印处理逻辑。
- 不调用现有邮件发送流程。
- 不依赖现有固定销售目录和文件命名。
- 使用部署服务器可用的中文字体，不能保留仅适用于 macOS 的硬编码字体路径。
- 支持并发生成并隔离临时文件。
- 日志不得记录完整验证码，不应无必要地输出完整手机号和订单号。

## 12. 输出文件

### 12.1 单项商品

单项商品输出一个 PDF，下载文件名固定为：

```text
PTE突击宝藏资料-WFD.pdf
PTE突击宝藏资料-DI.pdf
PTE突击宝藏资料-SST.pdf
PTE突击宝藏资料-RS.pdf
PTE突击宝藏资料-WE.pdf
```

文件名不包含订单号或手机号。

### 12.2 五项合集

合集入口对 5 个原始 PDF 分别添加同一手机号水印，然后打包为：

```text
PTE突击宝藏资料-五项合集.zip
```

ZIP 内包含：

```text
PTE突击宝藏资料-WFD.pdf
PTE突击宝藏资料-DI.pdf
PTE突击宝藏资料-SST.pdf
PTE突击宝藏资料-RS.pdf
PTE突击宝藏资料-WE.pdf
```

ZIP 不设置解压密码。

合集是独立商品入口。购买或登录合集不会自动获得五个单项入口权限。

## 13. 下载规则

- 只允许下载已经成功生成的文件。
- 同一成功文件在入口有效期内允许无限次下载。
- 重复下载不得触发重新生成。
- 下载请求必须验证有效浏览器会话或刚刚完成的手机号、验证码和原订单号登录。
- 公开下载链接不得直接暴露对象存储路径或本地文件路径。
- 下载应通过受控接口或短时签名地址完成。
- 下载文件使用本 SPEC 规定的用户可见文件名。
- `now >= expires_at` 后，所有公开下载立即停止。

失效页面文案：

> 链接已失效，有问题请直接联系小圆。

## 14. 文件和数据生命周期

### 14.1 原始文件

- 草稿阶段保存在非公开草稿目录。
- 发布后保存在对应资料版本的非公开源文件目录。
- 入口满 30 天失效后，原始 PDF 移入非公开历史目录。
- 原始 PDF 从入口失效时间起在 `history` 中继续保留 180 天，到期后由每日清理任务永久删除。
- 清理任务必须分批处理，单次最多清理 100 个文件，避免长时间占用磁盘和数据库锁。
- 永久删除原始 PDF 后，资料版本、原文件名、大小、页数、校验和、历史存储位置和清理时间等审计元数据继续保留。
- 数据库必须将已永久删除的原始文件明确标记为 `PURGED`，不能让记录继续表现为可读取文件。

### 14.2 买家生成文件

- 从文件成功生成时间起保存连续 720 小时。
- 每天执行一次定时归档任务。
- 归档任务将超过 720 小时的文件移动到独立的非公开 `old-sold` 目录。
- 归档是移动文件，不是永久删除。
- `old-sold` 由管理员后续手动清理。
- 数据库生成记录必须保留原存储位置、归档位置、生成时间、归档时间和状态。

链接有效期和文件保存期独立计算。例如文件在生成窗口末期创建时，链接会先于文件归档时间失效；链接失效后文件继续内部保存到自身归档时间，但买家不能继续下载。

### 14.3 数据库记录

本期以下数据库数据一直保存，不自动清理：

- 资料版本记录。
- 商品入口和链接记录。
- 手机号与订单号绑定。
- 生成任务记录。
- 文件记录。
- 归档记录。
- 必要的审计和错误记录。

浏览器会话仍须在链接失效时失效，数据库是否保留历史会话记录不影响其授权状态。

### 14.4 数据库备份

- 每天 `03:00 Asia/Shanghai` 执行一次 SQLite 在线备份，允许最多 5 分钟随机延迟；服务器停机错过任务后应在恢复时补跑。
- 使用 SQLite `.backup` 生成一致性快照。备份先写入 `/var/backups/prep-trove/.database-<UTC 时间>.sqlite3.tmp`，完成后再原子重命名为正式 `.sqlite3` 文件。
- 备份进程无论成功、失败或被中断，都必须通过退出清理删除本次临时文件；每次执行前同时清理超过 1 天的遗留 `.tmp` 文件。
- 正式数据库备份保留 14 天，正常稳定状态下最多保留约 15 个每日快照。
- 备份前必须检查可用空间，至少能够容纳一个当前数据库大小的新增快照和安全余量；空间不足时终止备份、记录错误并告警。
- 数据库备份只包含 SQLite 数据和文件元数据，不包含原始 PDF、生成文件、`history` 或 `old-sold`。
- 本地备份与业务数据位于同一服务器，不能作为完整灾备；生产环境必须同时使用阿里云快照、OSS 或异机备份覆盖数据库和私有文件目录。

## 15. 建议存储结构

以下为逻辑结构，实际实现可以使用本地磁盘或对象存储，但必须保持相同的隔离关系：

```text
materials/
└── <material-version-id>/
    ├── metadata
    ├── source/
    │   ├── WFD.pdf
    │   ├── DI.pdf
    │   ├── SST.pdf
    │   ├── RS.pdf
    │   └── WE.pdf
    └── generated/
        ├── WFD/
        ├── DI/
        ├── SST/
        ├── RS/
        ├── WE/
        └── BUNDLE/

history/
└── <material-version-id>/
    └── source/

old-sold/
└── <material-version-id>/
    └── <product-entry>/
```

`source`、`history`、`generated` 和 `old-sold` 均不得配置为公开静态目录。

## 16. 数据模型要求

以下为逻辑数据模型，字段名可按具体技术栈调整。

### 16.1 `material_versions`

- `id`
- `display_name`
- `status`
- `created_at`
- `published_at`
- `generation_deadline`
- `expires_at`
- `created_by`

### 16.2 `material_assets`

- `id`
- `material_version_id`
- `material_type`：WFD、DI、SST、RS、WE
- `source_storage_key`
- `original_filename`
- `file_size`
- `page_count`
- `checksum`
- `validation_status`
- `storage_status`：ACTIVE、HISTORY、PURGED
- `archived_at`
- `purged_at`
- `created_at`

同一版本和资料类型必须唯一。

### 16.3 `product_links`

- `id`
- `material_version_id`
- `product_entry`：WFD、DI、SST、RS、WE、BUNDLE
- `token_ciphertext`
- `token_hash`
- `created_at`

入口令牌至少使用 128 位安全随机数生成。为了让管理员在历史版本页重新复制链接，数据库需要保存可由服务端密钥解密的令牌密文，同时保存令牌摘要用于请求校验；令牌不得以明文写入数据库或日志。每个版本和入口只能有一个当前链接。

### 16.4 `buyer_bindings`

- `id`
- `material_version_id`
- `product_entry`
- `phone`
- `order_number`
- `status`
- `created_at`

强制唯一约束：

```text
UNIQUE(material_version_id, product_entry, phone)
UNIQUE(material_version_id, product_entry, order_number)
```

### 16.5 `generation_jobs`

- `id`
- `buyer_binding_id`
- `status`：PENDING、PROCESSING、SUCCEEDED、FAILED
- `error_code`
- `attempt_count`
- `created_at`
- `started_at`
- `completed_at`

同一绑定只能有一个成功结果。失败任务可以重试。

### 16.6 `generated_files`

- `id`
- `generation_job_id`
- `storage_key`
- `download_filename`
- `mime_type`
- `file_size`
- `checksum`
- `generated_at`
- `archive_at`
- `archived_at`
- `archive_storage_key`
- `status`：ACTIVE、ARCHIVED、MISSING、CORRUPTED

### 16.7 `buyer_sessions`

- `id`
- `buyer_binding_id`
- `token_hash`
- `created_at`
- `expires_at`
- `revoked_at`

### 16.8 `otp_challenges`

- `id`
- `phone`
- `code_hash`
- `purpose`
- `expires_at`
- `attempt_count`
- `consumed_at`
- `created_at`

## 17. 逻辑接口要求

具体 URL 和请求结构在技术设计阶段确定，系统至少需要覆盖以下逻辑能力。

### 17.1 管理后台

- 管理员登录和退出。
- 创建资料版本草稿。
- 上传或替换某题型 PDF。
- 获取草稿完整性和校验状态。
- 发布资料版本。
- 查询版本列表和状态。
- 查询某版本的 6 个入口链接。
- 查询基本生成记录。

### 17.2 买家端

- 读取入口页面状态。
- 请求短信验证码。
- 验证短信验证码。
- 提交订单号并登录或绑定。
- 创建或读取唯一生成任务。
- 轮询生成进度。
- 下载已生成文件。
- 退出当前入口登录状态。

所有授权规则必须在后端执行，不能依赖前端隐藏按钮实现。

## 18. 简单管理后台

### 18.1 登录

- 单管理员账号和密码。
- 账号和密码凭据通过服务器环境变量配置。
- 建议使用密码哈希环境变量，避免保存明文密码。
- 生产管理入口路径通过高熵环境变量配置，固定 `/admin` 不得暴露管理页面。
- 生产账号不得使用 `admin` 等默认值，首次安装生成不可预测账号和强随机密码。
- 不提供公开注册。
- 不提供找回密码。
- 不提供多角色或多管理员权限。
- 管理后台必须通过 HTTPS 访问。
- 登录接口需要限流和防暴力破解。

### 18.2 版本上传页

必须展示：

- 版本名称或创建时间。
- WFD、DI、SST、RS、WE 五个上传区域。
- 每个文件的上传进度和校验结果。
- 缺失文件提示。
- “发布”按钮。
- 发布前确认提示。

只有 5 个文件全部检查通过时，“发布”按钮才可用。

### 18.3 发布结果页

必须展示：

- 发布成功状态。
- 发布时间。
- 新增生成截止时间。
- 链接失效时间。
- 6 个带题型标识的入口链接。
- 每个链接的一键复制按钮。

### 18.4 历史版本页

至少展示：

- 版本名称或 ID。
- 发布时间。
- 生成截止时间。
- 链接失效时间。
- 当前状态。
- 6 个入口链接。
- 基本生成数量和成功数量。

## 19. 买家页面和视觉风格

### 19.1 目标感受

页面用于已经完成购买的用户提取资料，应该传达：

- 开心获得宝藏资料。
- 轻快、积极和有奖励感。
- PTE 冲刺和提分的行动氛围。
- 清晰可信的验证与下载过程。

目标用户以 20～35 岁、大学毕业及处于留学或 PTE 备考阶段的年轻人为主。

### 19.2 页面复用

6 个入口使用同一套结构和交互，仅切换：

- 题型名称。
- 主题色。
- 对应资料说明。
- 适当的装饰元素。

页面必须优先适配手机浏览器。

### 19.3 建议首屏文案

主标题：

> 恭喜宝宝，成功解锁 PTE 突击宝藏资料！

单项副标题示例：

> 这是你的 WFD 专属提分资料，完成验证后即可生成专属文件。

合集副标题示例：

> 五大题型一次集齐，接下来的每一步努力都算数。

### 19.4 页面结构

```text
品牌标识
→ 恭喜获得资料的主标题
→ 当前题型和资料说明
→ 手机号输入
→ 获取及填写短信验证码
→ 小红书订单号输入
→ 生成专属文件
→ 生成阶段进度
→ 下载按钮
→ 有效期与联系小圆提示
```

### 19.5 必要状态文案

仅限中国大陆手机号：

> 目前仅支持中国大陆手机号。没有中国大陆手机号，请联系小圆。

生成窗口结束且用户未生成：

> 本期资料的专属文件生成时间已经结束，仅限此前成功生成过文件的用户登录下载。

手机号已绑定另一个订单号：

> 该手机号已经绑定过订单号，请使用首次提交的订单号。如填写有误，请联系小圆。

订单号已绑定另一个手机号：

> 该订单号已经绑定过手机号，请使用首次绑定的手机号。如有问题，请联系小圆。

文件丢失或损坏：

> 文件暂时无法下载，请联系小圆处理。

链接失效：

> 链接已失效，有问题请直接联系小圆。

## 20. 隐私与安全

### 20.1 个人信息提示

系统收集并保存完整手机号和小红书订单号，并将完整手机号写入 PDF 每一页。生成前必须展示简短、清晰的提示，让用户了解上述用途。

建议文案：

> 为生成和保护你的专属资料，系统将保存手机号和订单号，并在资料每一页添加完整手机号水印。继续生成即表示你已了解上述用途。

### 20.2 文件安全

- 所有页面和接口必须使用 HTTPS。
- 原始 PDF、生成文件、历史目录和 `old-sold` 均不得公开暴露。
- 下载必须经过应用授权或使用短时签名 URL。
- 内部存储键不得由用户可控文件名直接拼接。
- 文件上传需要验证实际 PDF 内容，不能只检查扩展名。
- DI 原始 PDF 最大允许 100MiB；WFD、SST、RS、WE 原始 PDF 各自最大允许 50MiB。代理层请求体上限必须覆盖 100MiB 文件及 multipart 开销。
- 页面和状态接口仅在生成任务为 `SUCCEEDED`、私有文件记录有效、磁盘文件存在且实际大小与记录一致时显示生成成功。发现文件缺失或大小不一致时，必须将文件标记为 `MISSING`、任务降级为 `FAILED`，并允许同一绑定重新生成。
- ZIP 文件名和内部文件名必须由系统固定生成。

### 20.3 应用安全

- 管理员密码不得写入代码库。
- 生产管理入口、账号和密码必须分别使用高熵随机值，并在服务器私密文件中交付。
- 管理员登录需要限流。
- 买家 Cookie 使用安全属性并在服务端校验。
- 验证码、会话令牌和入口令牌不得明文写入日志。
- 对手机号、订单号和文件路径输入执行严格校验。
- 生成和下载接口必须防止越权访问其他版本、入口或买家文件。
- 未授权的程序化下载请求返回 `401`；浏览器直接访问未授权下载地址时使用 `303` 返回对应买家验证页，并显示一次性提示。
- 后端必须独立检查 10/30 天边界。

### 20.4 已接受风险

本期不验证订单真实性。因此，任何获得有效入口链接的人，在生成开放期内都可以填写一个格式正确的虚构订单号并尝试生成资料。手机号与订单号的一对一约束可以限制同一作用域内的重复绑定，但不能证明真实购买。

该风险已经明确接受，本期不增加订单数据导入或订单 API 校验。

## 21. 外部依赖与部署参考

### 21.1 域名与服务器

- 使用现有阿里云轻量应用服务器和域名，不使用 Serverless 计算平台。
- 买家端和管理后台部署在 `bzzl.ysspark.cn` 下。
- 部署配置可以参考相邻项目：Windows PowerShell 路径 `..\official-website`；Linux/macOS 路径 `../official-website`。
- 应用使用标准 Next.js Node.js 服务，由 systemd 管理，并通过 nginx 反向代理。
- 应用仅监听服务器回环地址，不直接向公网开放 Node.js 端口。
- 数据库使用服务器本地 SQLite，启用 WAL、事务、唯一约束和 `busy_timeout`。
- 原始资料、生成文件、历史资料和 `old-sold` 使用发布目录之外的私有本地数据目录。
- 每日归档和备份使用 systemd timer；代码发布、回滚不得覆盖业务数据目录。
- 新版本通过健康检查后，发布流程必须自动清理旧 release，只保留 `/opt/prep-trove/releases` 中最近 6 个完整发布目录。
- release 清理必须保护 `current` 当前指向目录；发布失败、健康检查失败或回滚过程中不得执行清理。
- 删除 release 前必须将目标解析为 `/opt/prep-trove/releases/<合法发布时间>` 下的真实目录，禁止删除 `current`、发布根目录、业务数据目录或路径范围之外的任何对象。
- SCP 与云助手发布通道必须复用同一 release 切换和清理逻辑，避免保留策略不一致。
- 服务器本地磁盘属于单点，生产环境必须启用轻量服务器快照或异机备份。
- 轻量服务器内存有限，单进程内的 PDF 水印生成默认串行执行，避免并发大文件处理耗尽内存。

### 21.2 阿里云短信

- 使用阿里云短信发送验证码。
- 参考已实测可接收验证码的相邻项目：Windows PowerShell 路径 `..\AIPTE\apps\DI`；Linux/macOS 路径 `../AIPTE/apps/DI`。
- AccessKey、短信签名和模板 ID 必须通过环境变量或服务器密钥管理配置。

### 21.3 水印脚本

- 参考：Windows PowerShell 路径 `..\AIPTE\knowledge\auto\add_waterprint.py`；Linux/macOS 路径 `../AIPTE/knowledge/auto/add_waterprint.py`。
- 复用水印算法前需要移除邮件、固定目录、CSV 和平台特定字体依赖。

## 22. 日志与可观测性

系统至少记录：

- 管理员登录成功与失败。
- 资料版本创建、文件上传和发布结果。
- 每个文件的检查结果。
- 短信发送成功、失败和限流事件。
- 验证成功与失败，不记录明文验证码。
- 绑定创建和唯一约束冲突。
- 生成任务开始、成功、失败和耗时。
- 文件下载结果。
- 每日归档任务结果。
- 文件丢失、校验失败和归档失败。

涉及手机号和订单号的普通日志应使用脱敏值。数据库业务记录按照本 SPEC 保存完整值。

## 23. 验收标准

### 23.1 管理后台

- [ ] 未登录用户无法访问管理后台。
- [ ] 生产固定 `/admin` 返回 404，只有配置的高熵路径显示管理页面。
- [ ] 登录请求缺少正确入口密钥时返回 404。
- [ ] 管理员可以通过环境变量配置的单账号密码登录。
- [ ] 管理员可以创建草稿并上传 5 个 PDF。
- [ ] 缺少任意一个 PDF 时不能发布。
- [ ] 加密、损坏或非 PDF 文件被拒绝。
- [ ] 点击发布后生成 6 个全新链接。
- [ ] 一周内连续发布多个版本时，每个版本链接均不同。
- [ ] 多个版本的 10/30 天时间独立计算。
- [ ] 后台能复制链接并查看关键时间与状态。

### 23.2 链接生命周期

- [ ] 发布后不足 240 小时时，新手机号可以获取验证码并生成。
- [ ] 到达 240 小时边界后，未生成手机号不能获取验证码。
- [ ] 240～720 小时内，已生成手机号可以重新验证并下载。
- [ ] 到达 720 小时边界后，所有买家访问和下载立即停止。
- [ ] 新版本发布不会改变历史版本链接、资料或有效期。
- [ ] 历史链接永远不能获取新版本资料。

### 23.3 手机号和验证码

- [ ] 只接受符合规则的中国大陆 11 位手机号。
- [ ] 页面明确提示没有中国大陆手机号时联系小圆。
- [ ] 验证码 5 分钟后失效。
- [ ] 发送频率和失败次数限制生效。
- [ ] 验证码不能重复使用。
- [ ] `DOWNLOAD_ONLY` 阶段未生成手机号不能收到验证码。

### 23.4 订单号与绑定

- [ ] 接受 `P` 加 18 位数字的订单号。
- [ ] 自动去除首尾空格并将小写 `p` 转换为大写。
- [ ] 拒绝长度或字符不符合规则的订单号。
- [ ] 同一版本和入口下，一个手机号只能绑定一个订单号。
- [ ] 同一版本和入口下，一个订单号只能绑定一个手机号。
- [ ] 同一手机号和订单号可以进入不同商品入口。
- [ ] 同一手机号和订单号可以进入不同资料版本。

### 23.5 生成与水印

- [ ] 单项入口生成一个对应题型 PDF。
- [ ] 合集入口生成包含 5 个 PDF 的 ZIP。
- [ ] 每一页均包含“祝考试好运 UPUP”。
- [ ] 每一页均包含完整 11 位手机号。
- [ ] 水印中不包含订单号。
- [ ] 生成文件名不包含手机号或订单号。
- [ ] 并发点击只产生一个成功文件。
- [ ] 生成失败允许重试。
- [ ] 已成功生成后不再重复运行水印流程。

### 23.6 登录与下载

- [ ] 同一浏览器再次打开同一入口时可以自动登录并下载。
- [ ] 换设备或 Cookie 丢失后必须验证手机号、验证码和原订单号。
- [ ] 登录其他版本或其他入口时需要单独验证。
- [ ] 有效期内允许无限次下载同一文件。
- [ ] 下载不会触发重新生成。
- [ ] 合集登录不会授予单项入口权限。
- [ ] 链接失效后历史 Cookie 不能继续下载。

### 23.7 文件和数据

- [ ] 原始文件和生成文件不能通过静态地址直接访问。
- [ ] 原始 PDF 在链接失效后进入 `history`，继续保留 180 天后由每日任务永久删除。
- [ ] 原始 PDF 永久删除后，数据库仍保留审计元数据，并将文件状态标记为 `PURGED`。
- [ ] 生成文件满 720 小时后由每日任务移入 `old-sold`。
- [ ] 归档后数据库仍保留完整生成和文件记录。
- [ ] 链接先失效而文件尚未归档时，买家仍不能下载。
- [ ] 数据库记录不会被本期定时任务自动删除。
- [ ] SQLite 每日备份先写临时文件再原子重命名，失败或中断不会遗留本次临时文件。
- [ ] 正式数据库备份保留 14 天，本地备份之外另有覆盖私有文件的阿里云快照、OSS 或异机备份。
- [ ] 成功发布后最多保留最近 6 个 release，当前版本受保护；发布失败或回滚时不清理 release。

## 24. 开发前提与最终决策摘要

1. 一次发布必须包含 WFD、DI、SST、RS、WE 五个 PDF。
2. 每次发布生成 6 个新链接，不复用历史链接。
3. 允许一周多次发布，所有版本独立计时并可以重叠有效。
4. 新生成开放 240 小时，链接总有效期 720 小时。
5. 240 小时后只有此前成功生成的用户可以验证并下载。
6. 只支持中国大陆 `+86` 的 11 位手机号。
7. 订单号格式为 `P` 加 18 位数字。
8. 在同一版本和入口下，手机号与订单号严格一对一。
9. 订单真实性本期不校验。
10. 自动登录以同一浏览器安全 Cookie 为准。
11. 换设备需要手机号、验证码和原订单号三项验证。
12. 每个绑定只能成功生成一次，后续复用原文件并允许无限次下载。
13. 水印为“祝考试好运 UPUP”与完整手机号，覆盖每一页。
14. 单项输出 PDF，合集输出包含 5 个 PDF 的 ZIP。
15. 下载文件名不包含手机号和订单号。
16. 生成文件满 720 小时后移动到 `old-sold`，不自动永久删除。
17. 数据库记录本期一直保存，不开发自动清理。
18. 后台采用单管理员账号和密码，通过环境变量配置。
19. 后台提供 5 个文件上传位置、发布按钮、链接复制和历史版本查看。
20. 已发布错误资料、错误绑定和国际手机号问题均由人工联系小圆处理。
